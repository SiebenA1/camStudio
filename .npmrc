# pnpm/npm 配置：把 store 与缓存重定向到工作区内，避免触碰系统全局缓存（沙箱限制）
store-dir=.pnpm-store
cache-dir=.pnpm-cache
# npm 缓存也重定向
cache=.npm-cache
# 不锁 registry（保留默认）
strict-peer-dependencies=false
auto-install-peers=true
